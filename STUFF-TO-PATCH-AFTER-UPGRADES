nikola.py
=========

(blog only) Nikola().render_template.replacer — deactivate (return dst)

post.py
=======

Post().text — add lead class, remove read more link

utils.py
========

generic_rss_renderer() — add link generation:


    pl = post.permalink(lang, absolute=True)
    plink = '/'.join((pl[:28], pl[29:31], pl[32:34], pl[35:-5], ''))
