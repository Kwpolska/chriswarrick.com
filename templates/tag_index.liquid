{% from 'helper' import renderIndex, renderPostHeader %}
{% layout "base.liquid" %}

<header class="page-header">
  <h1>{{ Title }}</h1>
  <a href="{{ Group.RssUrl }}">📣 RSS Feed</a>
</header>

{{ renderIndex("tagindex") }}

{% section head %}
  <link rel="alternate" type="application/rss+xml" title="RSS ({{ Title }})" hreflang="en" href="{{ Group.RssUrl }}">
{% endsection %}
